@page
@model OdeToFood.Pages.Resturants.ListModel
@{
}

<h1>Resturants</h1>

<form method="get">
    <div class="form-group">
        <div class="input-group">
            @*<input type="search" value="" name="searchTerm" class="form-control" />*@
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-btn">
                <button class="btn btn-default">
                    <i class="glyphicon glyphicon-search"></i>
                </button>
            </span>
        </div>
    </div>
</form>

@*render partial view*@
@foreach (var resturant in Model.Resturants)
{
    <partial name="_Summary" model="resturant" />
}


@*<table class="table">
    @foreach (var resturant in Model.Resturants)
    {
        <tr>
            <td>@resturant.Name</td>
            <td>@resturant.Location</td>
            <td>@resturant.Cuisine</td>
            <td>
                @*<a class="btn btn-lg" href="=/Resturants/Details?id=@resturant.ID">
                <a class="btn btn-lg" asp-page="./Details" asp-route-resturantID="@resturant.ID">
                    <i class="glyphicon glyphicon-zoom-in"></i>
                </a>
                <a class="btn btn-lg" asp-page="./Edit" asp-route-resturantID="@resturant.ID">
                    <i class="glyphicon glyphicon-edit"></i>
                </a>
                <a class="btn btn-lg" asp-page="./Delete" asp-route-resturantID="@resturant.ID">
                    <i class="glyphicon glyphicon-trash"></i>
                </a>
            </td>
        </tr>
    }
</table>*@

<a asp-page="./Edit" class="btn btn-primary">Add New</a>

@section footer{
    <div>@Model.message</div>
}

